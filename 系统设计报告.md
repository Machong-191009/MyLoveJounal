# 1 Our Love Journal - 情侣纪念网站 系统设计报告

## 1.1 项目概述

一个专属于两个人的私密纪念网站，用于保存和回顾你们在一起的各种珍贵瞬间——旅行、纪念日、吵架和好、日常甜蜜等。不同于社交媒体的公开属性，这是一个只属于你们两个人的私密空间。

---

## 1.2 市场调研：现有产品分析

### 1.2.1 商业应用

| 产品 | 类型 | 核心功能 | 缺点 |
|------|------|---------|------|
| **Between** | 情侣APP (韩国) | 私密聊天、共享日历、纪念日倒计时、相册、备忘录 | 已逐渐商业化，数据在别人服务器上，隐私风险 |
| **小恩爱** | 情侣APP (国内) | 情侣空间、相册、纪念日、树洞、游戏 | 广告多，功能臃肿，已走下坡路 |
| **恋爱记** | 情侣APP (国内) | 恋爱天数、纪念日提醒、情侣相册、日记 | 功能较简单，数据不可控 |
| **Lovedays** | 纪念日APP | 纪念日倒计时、周年提醒 | 只做倒计时，无相册功能 |
| **Coupled** | 情侣APP (海外) | 私密消息、共享列表、涂鸦、拥抱功能 | 缺少相册和时间线 |

### 1.2.2 开源项目

| 项目 | 技术栈 | 功能特点 | 不足 |
|------|--------|---------|------|
| **Love-Memory-Book** (GitHub: shxlzz) | React + CSS + localStorage | 数字日记本、媒体上传（照片/视频）、时间线视图、"我们的第一次"里程碑板块、4种主题皮肤、响应式设计 | 数据存浏览器本地(localStorage)不安全、无账号系统、无法多端同步、无真正的后端存储 |
| **SnapTales** (GitHub: mauryavishal767) | React + Vite + Tailwind | 共享时间线、每条回忆含标题/故事/时间/地点/日期/照片 | README很简陋、无详细功能文档、也是前端项目 |
| **love-story-website-template** (GitHub: vasudevks7) | 纯HTML | 交互式相册画廊、记忆小游戏、浪漫动画效果 | 纯静态页面、无数据管理、仅是模板 |

### 1.2.3 调研结论

**市面上的问题总结：**

1. **商业APP数据不可控**：你的照片和文字存在别人的服务器上，隐私风险大，且产品随时可能关停
2. **开源项目太简陋**：大多只是前端demo，用localStorage存数据，换个浏览器就没了
3. **缺乏真正的"自己的"体验**：没有一个既完整又私密、数据完全自己掌控的方案
4. **功能不够贴合实际**：很少有产品考虑到"吵架记录"、"情绪标签"这种真实恋爱中的场景

**我们的机会：做一个数据完全属于自己的、功能真正贴合情侣需求的私密网站。**

---

## 1.3 功能设计

### 1.3.1 核心功能模块

#### 1.3.1.1 模块一：时间线 (Timeline)
> 所有回忆按时间排列，一目了然

- 瀑布流/时间轴展示所有回忆
- 按年份、月份快速跳转
- 支持筛选和搜索
- 每条回忆包含：日期、标题、文字描述、照片/视频、地点、标签

#### 1.3.1.2 模块二：相册 (Albums)
> 按主题分类管理照片

- 自定义相册（如：旅行相册、日常相册、节日相册）
- 照片支持批量上传
- 照片浏览支持轮播、放大查看
- 支持为照片添加文字说明
- 相册封面自定义

#### 1.3.1.3 模块三：纪念日 (Anniversaries)
> 不要忘记每一个重要的日子

- 在一起天数实时显示
- 重要纪念日管理（认识的日子、在一起的日子、第一次旅行……）
- 纪念日倒计时/正计时
- 到期自动提醒（邮件/浏览器通知）
- 周年/月度自动计算

#### 1.3.1.4 模块四：里程碑 (Milestones / "我们的第一次")
> 记录每一个"第一次"

- 预设里程碑类型：第一次见面、第一次约会、第一次旅行、第一次吵架、第一次说"我爱你"等
- 支持自定义里程碑
- 每个里程碑可附带日期、故事、照片

#### 1.3.1.5 模块五：心情日记 (Mood Journal)
> 真实记录恋爱中的喜怒哀乐

- 每日/随时记录心情
- 心情标签：开心、感动、生气、吵架、想念、道歉、和好……
- 可以关联到具体事件
- 心情统计图表（月度/年度心情趋势）
- 双方都可以写，可以看到对方的

#### 1.3.1.6 模块六：地图足迹 (Map)
> 你们一起走过的地方

- 地图上标记去过的地方
- 点击标记查看关联的回忆/照片
- 足迹统计（去过几个城市、几个国家）

#### 1.3.1.7 模块七：愿望清单 (Wishlist)
> 想一起做的事

- 添加想一起做的事情
- 完成后可标记并关联回忆
- 分类管理（旅行、美食、体验……）

### 1.3.2 辅助功能

| 功能 | 说明 |
|------|------|
| **主题切换** | 多种视觉主题（温馨粉、简约白、暗夜黑等） |
| **双人账号** | 两个人各自的账号，数据共享 |
| **权限管理** | 某些日记可设为"仅自己可见" |
| **数据导出** | 支持导出为PDF纪念册/ZIP打包下载 |
| **搜索** | 全站搜索回忆、日记、照片 |
| **首页仪表盘** | 在一起天数、最近回忆、即将到来的纪念日、心情概览 |

---

## 1.4 系统架构设计

### 1.4.1 技术选型（确定方案）

```
框架:     Next.js 14+ (App Router) + TypeScript    -- 全栈方案，一个项目搞定前后端
样式:     Tailwind CSS                              -- 快速开发，响应式友好
ORM:      Prisma                                    -- 类型安全的数据库操作
数据库:   PostgreSQL                                 -- 结构化数据存储
文件存储:  本地文件系统（4T硬盘），后续可迁移至 S3  -- 照片/视频存储
认证:     Auth.js (NextAuth v5) + JWT               -- 登录认证 + 邀请码配对
部署:     Docker + Nginx                             -- 容器化一键部署
```

**为什么选 Next.js 全栈而不是前后端分离：**
1. **用户就两个人** — 不需要高并发后端，API Routes 完全够用
2. **一个项目** — 不用维护两个仓库，前后端共享类型定义，开发效率高
3. **一套语言** — TypeScript 贯穿前后端，不用切换语言上下文
4. **部署简单** — 一个 Docker 镜像搞定，不用协调多个服务
5. **生态丰富** — NextAuth.js、Prisma、uploadthing 等轮子现成可用
6. **体验好** — SSR 支持让首页、时间线等页面加载更快

### 1.4.2 系统架构图

```
┌─────────────────────────────────────────────────┐
│                   浏览器/手机                      │
│              (React / Next.js 前端)                │
└────────────────────┬────────────────────────────┘
                     │ HTTPS
                     ▼
┌─────────────────────────────────────────────────┐
│                  Nginx 反向代理                    │
│              (SSL证书 + 静态资源)                   │
└────────────────────┬────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────┐
│              后端 API 服务                         │
│         (Node.js / Go REST API)                   │
│                                                   │
│  ┌──────────┐ ┌──────────┐ ┌──────────────┐     │
│  │ 用户认证  │ │ 回忆管理  │ │ 文件上传处理  │     │
│  └──────────┘ └──────────┘ └──────────────┘     │
│  ┌──────────┐ ┌──────────┐ ┌──────────────┐     │
│  │ 相册管理  │ │ 纪念日   │ │ 通知服务      │     │
│  └──────────┘ └──────────┘ └──────────────┘     │
└───────┬──────────────────────────┬──────────────┘
        │                          │
        ▼                          ▼
┌──────────────┐         ┌──────────────────┐
│  PostgreSQL  │         │  MinIO/本地存储    │
│  (数据库)     │         │  (照片/视频)      │
└──────────────┘         └──────────────────┘
```

### 1.4.3 数据库设计（核心表）

```sql
-- 用户表
CREATE TABLE users (
    id          UUID PRIMARY KEY,
    username    VARCHAR(50) UNIQUE NOT NULL,
    email       VARCHAR(100) UNIQUE NOT NULL,
    password    VARCHAR(255) NOT NULL,    -- bcrypt加密
    avatar_url  VARCHAR(500),
    couple_id   UUID REFERENCES couples(id),
    created_at  TIMESTAMP DEFAULT NOW()
);

-- 情侣关系表
CREATE TABLE couples (
    id              UUID PRIMARY KEY,
    invite_code     VARCHAR(20) UNIQUE,       -- 邀请码
    together_since  DATE NOT NULL,            -- 在一起的日期
    created_at      TIMESTAMP DEFAULT NOW()
);

-- 回忆/时间线条目
CREATE TABLE memories (
    id          UUID PRIMARY KEY,
    couple_id   UUID REFERENCES couples(id),
    author_id   UUID REFERENCES users(id),
    title       VARCHAR(200) NOT NULL,
    content     TEXT,
    memory_date DATE NOT NULL,
    location    VARCHAR(200),
    latitude    DECIMAL(10, 8),
    longitude   DECIMAL(11, 8),
    mood        VARCHAR(50),                  -- 心情标签
    tags        TEXT[],                       -- 标签数组
    is_private  BOOLEAN DEFAULT FALSE,        -- 仅自己可见
    created_at  TIMESTAMP DEFAULT NOW(),
    updated_at  TIMESTAMP DEFAULT NOW()
);

-- 相册表
CREATE TABLE albums (
    id          UUID PRIMARY KEY,
    couple_id   UUID REFERENCES couples(id),
    name        VARCHAR(100) NOT NULL,
    description TEXT,
    cover_url   VARCHAR(500),
    created_at  TIMESTAMP DEFAULT NOW()
);

-- 照片/媒体文件表
CREATE TABLE media (
    id          UUID PRIMARY KEY,
    couple_id   UUID REFERENCES couples(id),
    memory_id   UUID REFERENCES memories(id),
    album_id    UUID REFERENCES albums(id),
    uploader_id UUID REFERENCES users(id),
    file_url    VARCHAR(500) NOT NULL,
    thumb_url   VARCHAR(500),                 -- 缩略图
    file_type   VARCHAR(20),                  -- image/video
    file_size   BIGINT,
    caption     TEXT,
    taken_at    TIMESTAMP,                    -- 拍摄时间(EXIF)
    created_at  TIMESTAMP DEFAULT NOW()
);

-- 纪念日表
CREATE TABLE anniversaries (
    id          UUID PRIMARY KEY,
    couple_id   UUID REFERENCES couples(id),
    title       VARCHAR(200) NOT NULL,
    date        DATE NOT NULL,
    repeat_type VARCHAR(20) DEFAULT 'yearly', -- yearly/monthly/once
    remind      BOOLEAN DEFAULT TRUE,
    note        TEXT,
    created_at  TIMESTAMP DEFAULT NOW()
);

-- 里程碑表
CREATE TABLE milestones (
    id          UUID PRIMARY KEY,
    couple_id   UUID REFERENCES couples(id),
    type        VARCHAR(50),                  -- first_meet/first_date/...
    custom_type VARCHAR(100),
    date        DATE,
    story       TEXT,
    created_at  TIMESTAMP DEFAULT NOW()
);

-- 愿望清单表
CREATE TABLE wishes (
    id           UUID PRIMARY KEY,
    couple_id    UUID REFERENCES couples(id),
    title        VARCHAR(200) NOT NULL,
    category     VARCHAR(50),
    is_completed BOOLEAN DEFAULT FALSE,
    completed_at TIMESTAMP,
    memory_id    UUID REFERENCES memories(id), -- 完成后关联的回忆
    created_at   TIMESTAMP DEFAULT NOW()
);
```

---

## 1.5 页面设计规划

### 1.5.1 页面列表

```
/                       首页仪表盘（在一起天数、最近回忆、纪念日提醒）
/login                  登录页
/register               注册页（含邀请码绑定）
/timeline               时间线（所有回忆按时间排列）
/timeline/:id           单条回忆详情
/albums                 相册列表
/albums/:id             相册详情（照片瀑布流）
/anniversaries          纪念日管理
/milestones             里程碑 / 我们的第一次
/journal                心情日记列表
/journal/new            写日记
/map                    地图足迹
/wishlist               愿望清单
/settings               设置（个人信息、主题、通知、数据导出）
```

### 1.5.2 首页仪表盘布局

```
┌─────────────────────────────────────────────┐
│           We've been together                │
│              ❤ 1314 天 ❤                    │
│         2021.08.14 — 至今                    │
├─────────────┬───────────────────────────────┤
│  即将到来    │       最近的回忆               │
│             │                               │
│ 🎂 生日     │  📸 [三亚旅行]   2025.10.01   │
│   还有23天   │  📸 [周年晚餐]   2025.08.14   │
│             │  📸 [一起做饭]   2025.07.22   │
│ 💍 周年     │                               │
│   还有89天   │                               │
├─────────────┴───────────────────────────────┤
│              本月心情概览                      │
│  😊😊😊😊😡😊😊😢😊😊😊😊😊😊😊            │
└─────────────────────────────────────────────┘
```

---

## 1.6 双人协作机制

### 1.6.1 注册与配对流程

```
用户A 注册 → 系统生成邀请码 → A把邀请码发给B → B注册时输入邀请码 → 两人绑定为情侣
```

### 1.6.2 数据共享规则

- 默认：双方可以看到所有回忆、相册、纪念日、里程碑、愿望清单
- 日记：可设为"仅自己可见"或"双方可见"
- 编辑权限：双方都可以新增/编辑/删除（操作记录可追溯）

---

## 1.7 隐私与安全设计

| 安全措施 | 实现方式 |
|---------|---------|
| 传输加密 | 全站HTTPS (Let's Encrypt免费证书) |
| 密码加密 | bcrypt哈希存储，永不明文 |
| 登录保护 | JWT Token + HttpOnly Cookie + 过期机制 |
| 文件安全 | 图片/视频不暴露真实路径，通过API中转访问 |
| 数据备份 | 定期自动备份数据库和文件到指定位置 |
| 自托管 | 所有数据存在自己的服务器/NAS上，不经过第三方 |
| 访问控制 | 仅配对的两人可访问，无公开页面 |

---

## 1.8 部署方案

### 1.8.1 方案A：自有服务器 / 云服务器

```yaml
# docker-compose.yml 示意
version: '3.8'
services:
  frontend:
    build: ./frontend
    ports:
      - "3000:3000"

  backend:
    build: ./backend
    ports:
      - "8080:8080"
    depends_on:
      - db
      - minio

  db:
    image: postgres:16
    volumes:
      - pg_data:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: love_journal
      POSTGRES_USER: lover
      POSTGRES_PASSWORD: your_secure_password

  minio:
    image: minio/minio
    volumes:
      - minio_data:/data
    command: server /data --console-address ":9001"

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./certs:/etc/nginx/certs

volumes:
  pg_data:
  minio_data:
```

### 1.8.2 方案B：NAS部署（群晖/威联通）

- Docker套件直接运行上述compose
- 内网穿透或域名DDNS访问
- 利用NAS的RAID保障数据安全

### 1.8.3 方案C：轻量方案（无服务器成本）

- 前端部署到 Vercel/Netlify（免费）
- 后端用 Supabase（免费PostgreSQL + 文件存储）
- 适合不想维护服务器的情况，但数据不完全自控

---

## 1.9 开发计划（建议分期）

### 1.9.1 Phase 1 - MVP（最小可用版本）—— 约2-3周
- [x] 项目初始化、技术栈搭建
- [ ] 用户注册/登录 + 邀请码配对
- [ ] 首页仪表盘（在一起天数）
- [ ] 时间线：创建/查看/删除回忆（含图片上传）
- [ ] 基本的纪念日管理

### 1.9.2 Phase 2 - 完善体验 —— 约2周
- [ ] 相册功能（创建相册、批量上传、瀑布流浏览）
- [ ] 里程碑 / 我们的第一次
- [ ] 主题切换
- [ ] 移动端适配优化

### 1.9.3 Phase 3 - 高级功能 —— 约2-3周
- [ ] 心情日记 + 心情统计图表
- [ ] 地图足迹
- [ ] 愿望清单
- [ ] 纪念日提醒通知
- [ ] 数据导出（PDF/ZIP）

### 1.9.4 Phase 4 - 锦上添花 —— 持续迭代
- [ ] 情侣小游戏（问答、默契测试）
- [ ] 每日一句情话
- [ ] 信件/胶囊功能（定时解锁）
- [ ] 音乐播放器（我们的歌单）
- [ ] AI自动生成年度回忆总结

---

## 1.10 总结

这个项目的核心价值在于 **"数据完全属于自己"** 和 **"功能真正贴合真实恋爱场景"**。

市面上的产品要么是商业APP（数据在别人手里，随时可能关停），要么是过于简陋的开源demo（localStorage存数据，换个浏览器就没了）。我们要做的是：

1. **完整的全栈应用**：有真正的后端和数据库，数据安全持久
2. **自托管部署**：照片和数据存在自己的服务器上，隐私有保障
3. **真实场景覆盖**：不只是甜蜜的部分，吵架、和好、情绪波动都是恋爱的一部分
4. **双人协作**：两个人都能记录，共同维护这段回忆

这不只是一个网站，而是你们爱情故事的数字化载体。
